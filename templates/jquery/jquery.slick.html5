(function($){
	$(document).ready(function(){
		var slick  = $('.<?php echo $this->cssClass;?>');

		// initialize slick, if more than one slide
		if(slick.children().length > 1){
			var slick = $('.<?php echo $this->cssClass;?>').slick({
				<?php echo $this->config;?>
			});
		}
	});
})(jQuery);
